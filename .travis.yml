language: ruby
notifications:
  email: false

sudo: false

dist: trusty

addons:
  apt:
    sources:
      - chef-stable-trusty
    packages:
      - chefdk

bundler_args: --without='vagrant'

install:
  - cd ./server-base
  - gem install bundler

branches:
  only:
    - master

services: docker

env:
  - INSTANCE=ubuntu-1604

before_script:
  - eval "$(/opt/chefdk/bin/chef shell-init bash)"
  - /opt/chefdk/embedded/bin/chef --version
  - /opt/chefdk/embedded/bin/cookstyle --version
  - /opt/chefdk/embedded/bin/foodcritic --version

script:
  # Prepare OpenSSH environment to work around error when using Docker
  - mkdir /var/run/sshd
  # Run index to make some tetchy gems work
  - bundle --full-index
  # Test suite
  - bundle exec rake travis
